#include <sys/types.h>
#include <sys/stat.h>
#include <unistd.h>
#include <stdlib.h>
#include <algorithm>
#include <signal.h>
#include <iostream>
#include <string>

using namespace std;

// function delclarations


// structs


// defines

int main()
{
   // ensure you receive proper arguments/input

   // create a process to send to maxima

   // create a process to receive from maxima. make sure you are synced up

   // make sure all processes are closed down properly

   // display the answer back to the user in a formated way

    return 0;
}

// prompt user for input 

// open pipe(not popen) to send to maxima

// open pipe to read data from maxima

// format the data for the user

